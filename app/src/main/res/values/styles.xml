<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="main" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:gravity">center</item>
        <item name="android:background">#FFFFFF</item>

    </style>

    <style name="main2" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="StarRatingBar" parent="RatingBar">
        <item name="android:progressDrawable">@drawable/rationg_bar</item>
        <item name="android:isIndicator">true</item>
    </style>

    <style name="RatingBar">
        <item name="android:indeterminateOnly">false</item>
        <item name="minHeight">57dip</item>
        <item name="maxHeight">57dip</item>
        <item name="android:mirrorForRtl">true</item>
    </style>

    <style name="TabLayoutTextBold">

        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>

    </style>

</resources>